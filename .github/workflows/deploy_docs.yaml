name: Build and Deploy Documentation
on:
  push:
    branches:
      - master
      - test-tagging

jobs:
  Build-Documentation:
    uses: NWChemEx-Project/.github/.github/workflows/deploy_docs_tmpl.yaml@build-doc-test
    with: 
      target: tensorwrapper_cxx_api
      dependencies: cmake doxygen sphinx
